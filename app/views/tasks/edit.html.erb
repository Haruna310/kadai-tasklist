<h1>
 リストを編集
</h1>

<%= form_with(model: @task, local: true) do |f| %>

<%= f.label :title %>

<%= f.text_field :title %>

<%= f.submit '更新' %>

<% end %>

<%= link_to '一覧に戻る', tasks_path %>

<%= link_to '削除する', @task, method: :delete, data: { confirm: '削除してよろしいですか？'} %>