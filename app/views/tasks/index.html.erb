<h1> リスト</h1>

<h2>リスト名</h2>
    <ul>
        <% @tasks.each do |task| %> 
        <li><%= task.status %>
            <!--&gt;-->
        <%= task.content %>
        <%= link_to '編集', edit_task_path(task) %></li>
        <% end %>
    </ul>

<%= link_to '追加', new_task_path %>